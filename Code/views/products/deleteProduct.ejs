<!DOCTYPE html>
<html lang="en">
<head>
    <!-- head --> 
    <%- include('../partials/head') %>
    <!-- /head -->
    <title>Eliminar producto</title>
</head>
<body>
    <!-- header --> 
    <%- include('../partials/header', {selected: ''}) %>
    <!-- /header --> 
    <main>
        <h2>Inroduce el ID para eliminar el producto</h2>
        <div id="main-register">
            <div id="form-container-register">
                <form action="/product/delete?_method=DELETE" method="POST">
                    <div class="form-control-delete">
                        <input type="text" name="id" placeholder="id del producto" minlength="13">
                    </div>
                    <div id="button-register">
                        <button type="submit">enviar</button>
                    </div>
                </form>
            </div>
        </div>
        <% if (locals.id) { %>
            <h2 style="color: red;">No existe producto con id: <%= id %></h2>
        <% } %>
    </main>
    <!-- footer --> 
    <%- include('../partials/footer') %>
    <!-- /footer --> 
</body>
</html>